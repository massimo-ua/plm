const PaymentQuery = require('./QuerySchema');
const PaymentMutation = require('./MutationSchema');
const Payment = require('./Payment');

module.exports = {
  PaymentQuery,
  PaymentMutation,
  Payment,
};
