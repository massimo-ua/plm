const Create = require('./Create');
const Update = require('./Update');
const Delete = require('./Delete');
const SetAsHome = require('./SetAsHome');

module.exports = {
  Create,
  Update,
  Delete,
  SetAsHome,
};
